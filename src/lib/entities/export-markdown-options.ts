export class ExportMarkdownOptions {
  /**
   * whether increase any markdown heading levels within the content of a section
   */
  increseContentMarkdownLevels: boolean = true;
  /**
   * whether add stats to the markdown
   * @default true
   */
  addWritingPlanStats: boolean = true;
}
